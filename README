# FNA3D_HLSL

3D 图形库，为 [FNA](https://fna-xna.github.io/) 提供 Graphics 命名空间实现。这是 FNA3D 的 DXC/SPIR-V/Vulkan 后端分支，通过 SDL_GPU 渲染。

**与上游 [FNA3D](https://github.com/FNA-XNA/FNA3D) 的差异**：

| | 上游 FNA3D | FNA3D_HLSL |
|---|---|---|
| 渲染后端 | OpenGL / D3D11 / SDL_GPU | **仅 SDL_GPU (Vulkan)** |
| Shader 格式 | .fxb (MojoShader) | **.feb (FNA3D Effect Binary)** |
| Shader 编译 | fxc / MojoShader | **DXC → SPIR-V** |
| 平台项目 | visualc/, Xcode/ | **全部移除** |

## Shader 管线

```
HLSL 源码 (.hlsl)
  → DXC -spirv -T vs_6_0 / ps_6_0
    → SPIR-V 二进制
      → FEB Builder 打包
        → .feb 文件（FNA3D Effect Binary）
          → FNA3D_CreateEffect() 加载
            → SDL_GPU (Vulkan) 直接消费 SPIR-V，创建 Shader → Pipeline → 绘制
```

### FEB 二进制格式

FEB (FNA3D Effect Binary) 是自定义 shader 打包格式，magic number `0x42414E46` ("FNAB")，version 1。

**二进制布局**（所有整数为 little-endian）：

```
Header (64 字节 = 16 × uint32):
  [ 0] magic            0x42414E46 ("FNAB")
  [ 1] version          1
  [ 2] techniqueCount
  [ 3] passCount
  [ 4] paramCount
  [ 5] shaderCount
  [ 6] stringTableSize
  [ 7] paramOffset
  [ 8] techniqueOffset
  [ 9] passOffset
  [10] shaderOffset
  [11] spirvOffset
  [12] totalSize        （必须等于文件实际大小）
  [13-15] reserved (0)

String Table:   null-terminated 字符串，追加在 header 之后
Parameters:     每个参数 84+ 字节（nameOff + semOff + type + regIdx + defaultValue(64B) + annotations）
Techniques:     每个 16 字节（nameOff + passStart + passCount + annotationCount）
Passes:         每个 24 字节（nameOff + vsIdx + psIdx + renderStateCount + samplerStateCount + reserved）
Shaders:        每个 24 字节（stage + entryOff + spirvOff + spirvSize + samplerCount + uniformBufferCount）
SPIR-V Data:    原始 SPIR-V 二进制
```

详见 `src/FNA3D_Effect.h` 和 `src/FNA3D_Effect.c` 中的解析器实现。

### 构建 FEB 文件

使用 [FNA3D_HLSL_Test](../FNA3D_HLSL_Test/) 中的 `tools/feb_builder.py`：

```bash
python3 tools/feb_builder.py manifest.feb.json
```

Manifest 格式（`.feb.json`）及着色器编写约定详见[测试项目 README](../FNA3D_HLSL_Test/README.md)。

## 依赖

- **运行时**：SDL 3.2.0+（唯一依赖）。设备创建仅请求 SPIR-V shader 格式，因此 SDL_GPU 实际只会选择 Vulkan 驱动；系统必须有可用的 Vulkan 驱动，否则设备创建失败。
- **构建时**：DXC（DirectX Shader Compiler），仅用于内容管线将 HLSL 编译为 SPIR-V。FNA3D 本身不依赖 DXC。
- 不直接调用 C 运行时（所有内存操作通过 SDL_malloc/SDL_free）

## 支持的平台

本分支为 **Vulkan 单后端**：shader 只以 SPIR-V 形式提供，设备创建只声明 SPIR-V 格式，SDL_GPU 的 D3D12 / Metal 驱动因不消费 SPIR-V 而不会被选中。

| 平台 | 图形驱动 | 状态 |
|------|---------|------|
| Linux | Vulkan (radv/AMDVLK) | 已测试 |
| Windows | Vulkan（厂商驱动） | 待测试 |
| macOS / iOS | 不支持（SDL_GPU 无 Vulkan-on-Metal 路径） | — |

## 构建

```bash
cmake -B build -G Ninja . -DCMAKE_BUILD_TYPE=Release
ninja -C build

# 静态库
cmake -B build -G Ninja . -DBUILD_SHARED_LIBS=OFF

# 关闭 Dear ImGui 集成（默认 ON）
cmake -B build -G Ninja . -DFNA3D_IMGUI=OFF
```

## 测试

1. 构建本库
2. 运行 [FNA3D_HLSL_Test](../FNA3D_HLSL_Test/) 中的 C 渲染测试：

```bash
cd ../FNA3D_HLSL_Test
./build_linux.sh
./out_linux/triangle_test
```

3. 端到端 C# 集成测试位于 [FNA_Test](../FNA_Test/)：

```bash
cd ../FNA_Test
./run_tests.sh
```

## Dear ImGui 集成（可选，`FNA3D_IMGUI`，默认 ON）

FNA3D 可将 [Dear ImGui](https://github.com/ocornut/imgui) 直接编译进输出库，让本库的使用者无需接触 SDL_GPU 即可绘制 ImGui 叠加界面。

- ImGui 的 **SDL3 + SDL_GPU 后端由 FNA3D 内部驱动**，只对外暴露少量生命周期函数。
- 通过 [dear_bindings](https://github.com/dearimgui/dear_bindings) 生成扁平 C ABI (`dcimgui`)，把完整 ImGui 控件 API 以 `extern "C"` 的 `ImGui_*` 符号导出，供 C / C# P/Invoke 调用。
- 关闭 `FNA3D_IMGUI` 时，库退回**纯 C**（不链接 libstdc++、无 ImGui 符号），`FNA3D_ImGui_*EXT` 变为安全空操作。

子模块位于 `thirdparty/imgui`、`thirdparty/dear_bindings`。**构建期需要 `python3` + `ply`**（运行 dear_bindings 生成绑定）；不想要该依赖就配置 `-DFNA3D_IMGUI=OFF`。

### 头文件

```c
#include <FNA3D.h>
#include <FNA3D_ImGui.h>   /* 生命周期：FNA3D_ImGui_*EXT */
#include "dcimgui.h"       /* ImGui 控件 API：ImGui_Begin / ImGui_Text / ... */
```

`dcimgui.h` 由本库构建时生成（`build/generated/dcimgui.h`），是 C 兼容的；同时安装 `dcimgui.json`，C# 侧可据此生成完全匹配的 P/Invoke 绑定。

### 每帧调用顺序

```c
FNA3D_ImGui_InitEXT(device);          /* 一次，必须在 CreateDevice 之后 */
/* ---- 每帧 ---- */
FNA3D_ImGui_NewFrameEXT(device);      /* 先调用，再构建 UI */
ImGui_ShowDemoWindow(NULL);
FNA3D_Clear(...);                     /* 正常绘制 */
/* ... 绘制场景 ... */
FNA3D_SwapBuffers(...);               /* ImGui 在此自动叠加渲染 */
/* ---- 退出 ---- */
FNA3D_ImGui_ShutdownEXT(device);      /* 必须在 DestroyDevice 之前 */
FNA3D_DestroyDevice(device);
```

**关键约束**：不要自己调 `ImGui_Render()` / `ImGui_EndFrame()` / `ImGui_CreateContext()`——FNA3D 内部已处理。不要与内置 event watch 同时使用 `ProcessEventEXT`。

### 注意事项

- **线程**：所有 FNA3D 与 ImGui 调用须在同一线程。
- **共享库 vs 静态库**：静态库 + `FNA3D_IMGUI=ON` 时 C 程序需 `-lstdc++`。
- **depth clip 补丁**：vendored 的 ImGui SDL_GPU 后端默认 `enable_depth_clip=false`，与本库 `depthClamp=false` 不兼容。CMake 配置阶段自动应用 `patches/0001-sdlgpu3-enable-depth-clip.patch` 修复。详见 `patches/README.md`。

## 已知限制

- **FEB 参数条目 stride** — `FNA3D_LoadEffect()` 的 stride 解析存在已知问题，但当前所有 stock effect 工作正常。
- **macOS/iOS 不支持** — SDL_GPU 无 Vulkan-on-Metal 路径。

## 已移除的组件

- MojoShader（git submodule + 所有引用）
- D3D11 后端（`src/FNA3D_Driver_D3D11.*`）
- OpenGL 后端（`src/FNA3D_Driver_OpenGL.*`）
- Tracing 子系统（`src/FNA3D_Tracing.*`、`replay/`、`dumpspirv/`）
- Visual Studio 项目（`visualc/`、`visualc-gdk/`）
- Xcode 项目（`Xcode/`）

## 代码约定

- **C 方言**：`-std=gnu99`，`-Wall -Wno-strict-aliasing -pedantic`
- **缩进**：Tab（tabstop=8），不用空格
- **命名**：公开 API 使用 `FNA3D_` 前缀
- **内存**：通过 `SDL_malloc`/`SDL_free`/`SDL_calloc`，不直接用 C 运行时
- **资源释放**：命名为 `AddDispose*`（非 `Destroy*`）

## License

zlib license，详见 LICENSE。
